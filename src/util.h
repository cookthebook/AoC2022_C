#ifndef _UTIL_H_
#define _UTIL_H_

#include <stdio.h>

/* strip whitespace characters off the end of str
 * returns the resulting strlen of the str */
size_t util_strip(char *str);

#endif /* _UTIL_H_ */